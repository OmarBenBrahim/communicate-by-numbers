# JWT secret for token signing - CHANGE IN PRODUCTION
JWT_SECRET=dev-secret-key-change-in-production

# Node environment
NODE_ENV=development

# Server port
PORT=3000

# API URL (used by client-side code)
NEXT_PUBLIC_API_URL=http://localhost:3000
